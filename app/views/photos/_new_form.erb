<%= form_for Photo.new, remote: true, :html => { :multipart => true, :id => "fileupload"  } do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="image field">
	  <%= f.file_field :image%>
  </div>
	<div class="action">
		<a href="#", class="button", id="photo-add-button">Add Image</a>
	</div>
<% end %>